# -*- coding: utf-8 -*-
ListaVal=[]
for i in range(5):
    val=int(input())
    if (val%2==0):
        ListaVal.append(val)
        QuantValPosPares=len(ListaVal)
print("%d valores pares"%QuantValPosPares)